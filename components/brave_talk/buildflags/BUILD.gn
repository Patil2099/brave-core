import("buildflags.gni")
import("//build/buildflag_header.gni")
import("//brave/components/brave_talk/buildflags/buildflags.gni")

buildflag_header("buildflags") {
  header = "buildflags.h"
  flags = [
    "BRAVE_TALK_ENABLED=$brave_talk_enabled",
  ]
}
